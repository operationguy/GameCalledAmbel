z = {
	ac : 0,
	zoom : 1,
	base : 1
}

base_depth = depth;

hover = false
toggle = true;
toggled = false;
held = false;

image_xscale = z.base;
image_yscale = z.base;

// ITEM EXCLUSIVES //

max_stack = get_max_stack(iid);